#include<stdio.h>
#include<conio.h>
#include<stdlib.h>
#include<string.h>
#include<sys/time.h>
#include<stdlib.h>
int main()
{
	// 1. Write a menu driven program for dynamic memory allocation for the item insertion and deletion using malloc and realloc functions.
	int *a,*b,c;
	do
	{
		printf("\n Enter 1 for dynamic memory allocation \n");
		printf("\n Enter 2 for delete ");
		scanf("%d",&c);
		switch(c)
		{
			case 1: printf("Enter the ");
		}
	}
	while(1);
	getch();
	return 0;
}
